# Optional. Leave blank to keep the local (device) store.
# Add these in Vercel when you create the Supabase project.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Vuna Gift paybill (Daraja C2B). Demo uses 400200 until this is live.
# VITE_VUNA_PAYBILL=400200
